@model IEnumerable<MLB_Teams.Models.MLBTeam>
@{
    ViewBag.Title = "Index";
}

<h2>MLB Teams</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.City)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StadiumName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstYear)
        </th>
        <th></th>
    </tr>
    @foreach(var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelIem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StadiumName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstYear)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TeamID})
                @Html.ActionLink("Details", "Details", new { id = item.TeamID})
                @Html.ActionLink("Delete", "Delete", new { id = item.TeamID})
            </td>
        </tr>
    }
</table>

